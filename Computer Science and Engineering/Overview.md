boolean algebra, login gates and circuits, karnaugh maps

transistors,

theory of computation - deterministic and non-deterministic machines, adding a stack, turing machines, hierarchy of grammars

architecture - memory, compute(arithmetic and logic unit), fetch-decode-execute cycles, assembly languages, low-level languages, compilers

data structures and algorithms

database management systems - relational dbs

operating systems - kernel and user space, compute and memory, levels of memory, memory management, scheduling and process management, basic user space programs, interprocess communication

philosophies of languages - object-based(C), object oriented(C++), interpreted  and compiled languages

internet and networking, browsers

cloud computing

artificial intelligence

blockchains